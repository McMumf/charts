apiVersion: v1
kind: Service
metadata:
  name: immich-ml
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "common.labels" . | nindent 4 }}
    app: immich-ml
spec:
  type: ClusterIP
  ports:
    - port: 3003
      protocol: TCP
  selector:
    app: immich-ml
